package com.example.carsyroad;

import org.junit.Test;
import static org.junit.Assert.assertEquals;
import android.content.Context;

public class S3VerifyObstacleMovementTest {
  /*  Context game;
    Driver driver = new Driver(game,6);

    //checks if obstacle 1 can move across the screen
    @Test
    public void testObstacle1Movement() {
        Obstacle obstacle = driver.getObstacle(0);
        int oldX = (obstacle.getX())[1];
        obstacle.move();
        int newX = (obstacle.getX())[0];

        assertEquals(newX, (oldX + 1) % 9);
    }

    //checks if obstacle 2 can move across the screen
    @Test
    public void testObstacle2Movement() {
        Obstacle obstacle = driver.getObstacle(1);
        int oldX = (obstacle.getX())[0];
        obstacle.move();
        int newX = (obstacle.getX())[0];

        assertEquals(newX, (oldX - 2) % 9);
    }

    //checks if obstacle 3 can move across the screen
    @Test
    public void testObstacle3Movement() {
        Obstacle obstacle = driver.getObstacle(3);
        int oldX = (obstacle.getX())[0];
        obstacle.move();
        int newX = (obstacle.getX())[0];

        assertEquals(newX, (oldX + 2) % 9);
    }*/
}
