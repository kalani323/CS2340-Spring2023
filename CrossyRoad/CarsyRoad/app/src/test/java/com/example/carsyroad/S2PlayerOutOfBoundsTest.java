package com.example.carsyroad;

import org.junit.Test;
import static org.junit.Assert.assertEquals;

import android.content.Context;

public class S2PlayerOutOfBoundsTest {
  /*  Context gameActivity;
    Driver driver = new Driver(gameActivity,  6);
    Character character = driver.getCharacter();

    @Test
    public void testUpBounds() {
        // need to move the sprite to the top of the screen
        for (int i = 0; i < 14; i++) {
            driver.moveCharacter(0, 1);
        }

        // now that he's at the top let's make sure he can't move any more
        int oldY = character.getY();
        driver.moveCharacter(0, 1);
        int newY = character.getY();

        assertEquals(oldY, newY);
    }

    @Test
    public void testDownBounds() {
        // character starts at the bottom of the screen so we can test it directly

        int oldY = character.getY();
        driver.moveCharacter(0, -1);
        int newY = character.getY();

        assertEquals(oldY, newY);
    }

    @Test
    public void testLeftBounds() {
        // first move the character to the left boundary
        for (int i = 0; i < 4; i++) {
            driver.moveCharacter(-1, 0);
        }

        int oldX = character.getX();
        driver.moveCharacter(-1, 0);
        int newX = character.getX();

        assertEquals(oldX, newX);
    }

    @Test
    public void testRightBounds() {
        // first move the character to the right boundary
        for (int i = 0; i < 4; i++) {
            driver.moveCharacter(1, 0);
        }

        int oldX = character.getX();
        driver.moveCharacter(1, 0);
        int newX = character.getX();

        assertEquals(oldX, newX);
    }*/
}
